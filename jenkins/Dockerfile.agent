# Jenkins Agent Dockerfile for Smart Toll Cache System
# Includes Java 21, Node.js, Python 3.10, Docker CLI

FROM jenkins/inbound-agent:latest

USER root

# Install build tools
# RUN apt-get update && apt-get install -y openjdk-21-jdk nodejs python3.10 docker-ce-cli

USER jenkins
