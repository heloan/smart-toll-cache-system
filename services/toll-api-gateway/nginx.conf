# NGINX configuration for Smart Toll Cache System
# Placeholder — configure upstream servers and load balancing

# upstream toll_backend {
#     # Load balancing algorithm: round-robin (default)
#     server toll-service-1:8080;
#     server toll-service-2:8080;
#     server toll-service-3:8080;
# }
#
# server {
#     listen 80;
#     server_name localhost;
#
#     location /api/ {
#         proxy_pass http://toll_backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
#
#     location / {
#         proxy_pass http://toll-frontend:3000;
#     }
# }
